import type { AstroI18nextConfig } from "astro-i18next";

export default {
    defaultLocale: "en",
    locales: ["en"],
    defaultNamespace: "translation",
    namespaces: [
        "translation",
    ],
} as AstroI18nextConfig;